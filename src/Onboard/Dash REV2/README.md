# IMPORTANT

When pulling this branch, the lib folder will not have the right library overrides. Unzip lib.zip and place the contents into the lib folder. Otherwise the alphanumerics and CAN bus will straight up not work.